import React,{Component} from 'react';

export class Header extends Component {
    render(){
        return(
            <div> Header named (no default)</div>
        );

    }
}

export class Footer extends Component{
    render(){
        return(
            <div> Footer named (no default)</div>
        );

    }
}